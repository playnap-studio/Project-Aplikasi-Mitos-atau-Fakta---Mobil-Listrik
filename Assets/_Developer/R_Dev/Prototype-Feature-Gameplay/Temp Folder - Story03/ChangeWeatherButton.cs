using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UniStorm;

public class ChangeWeatherButton : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //UniStorm.UniStormManager.Instance.ChangeWeatherInstantly(UniStorm.Instance.WeatherType weatherType);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
